apiVersion: v1
kind: Service
metadata:
  name: uir-api
  labels:
    app: uir-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: uir-api