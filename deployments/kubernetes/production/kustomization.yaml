apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
  - ../

namespace: uir-production

commonLabels:
  environment: production

patchesStrategicMerge:
  - deployment-patch.yaml

configMapGenerator:
  - name: uir-config
    behavior: merge
    literals:
      - log-level=WARNING